# calculatior

browser JS evaluates user expressions. I replace ^ with ** and map common functions to Math.*. This is standard for a browser calculator; avoid entering arbitrary JS you don't trust. The graphing is basic (no symbolic algebra), but good for quick plotting of functions.
